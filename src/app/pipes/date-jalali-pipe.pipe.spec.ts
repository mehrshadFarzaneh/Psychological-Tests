import { DateJalaliPipePipe } from './date-jalali-pipe.pipe';

describe('DateJalaliPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateJalaliPipePipe();
    expect(pipe).toBeTruthy();
  });
});
